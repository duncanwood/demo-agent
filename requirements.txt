# demo-agent — cloud-default runtime deps (verified-working set, 2026-07-16, Python 3.11+)
# [runner] pulls the pipecat dev server (fastapi/uvicorn + prebuilt client UI).
pipecat-ai[webrtc,runner,openai,deepgram,cartesia,silero]==1.5.0
playwright>=1.45
python-dotenv>=1.0
